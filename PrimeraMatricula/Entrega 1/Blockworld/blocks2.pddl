(define (problem blocks1)
    (:domain blocks)
    (:objects A B C - block)
    (:init  (ontable A)
            (ontable B)
            (clear C)
            (clear C)
            (on C A)
            (handempty)
    )
    (:goal  (and
                (on B C)
                (on A B)
            )
    )
)